<div class="container pt-3">
  <div class="row">
    <div class="col-md-3">
      <%= render 'list' %>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
      <table class="table">
        <tbody>
          <tr>
            <td>a</br><%= link_to @book.user.name , user_path(@book.user_id) %></td><!--<td>userの画像貼り付け</td>-->
            <td><%= link_to @book.title, book_path(@book.id) %></td>
            <td><%= @book.body %></td>
            <% if current_user.id == @book.user_id %>
              <td><%= link_to "Edit", edit_book_path(@book.id) , class:"btn-success btn-sm" %></td>
              <td><%= link_to "Destroy", book_path(@book), method: :delete , class:"btn-danger btn-sm" %></td>
            <% end %>
          </tr>
        </tbody>
      </table>


    </div>
  </div>
</div>